[project]
name = "notification-prioritizer"
version = "0.1.0"
description = "OpenEnv Notification Prioritizer"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
